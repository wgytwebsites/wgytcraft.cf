language:
  node_js
before_install:
    - npm install -g codecov
    - npm install -g istanbul
script:
    - istanbul cover ./node_modules/mocha/bin/_mocha --reporter lconvonly -- -R spec
    - codecov
